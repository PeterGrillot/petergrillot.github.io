{
  "name": "petergrillot.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon",
    "build": "npx tsx ./lib/build.ts",
    "clean": "npx tsx ./lib/clean.ts",
    "dev": "DEV_MODE=true npm run build",
    "deploy": "npm run build && gh-pages -d build",
    "deploy:gh-pages": "npm run build && git stash && git checkout gh-pages && git pull && git rm -r . && git checkout main -- build && mv build/* . && rm -rf build && git add -A . && git commit -m 'Deploy gh-pages' && git push origin gh-pages && git checkout main"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown-it": "^12.3.2"
  },
  "devDependencies": {
    "@types/ejs": "^3.1.0",
    "@types/markdown-it": "^12.2.3",
    "@types/node": "^17.0.23",
    "ejs": "^4.0.1",
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.0",
    "nodemon": "^3.1.4",
    "tsconfig-paths": "^3.14.1",
    "tslib": "^2.3.1",
    "tsx": "^4.19.3",
    "typescript": "^4.6.3"
  }
}
